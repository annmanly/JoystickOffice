.event-grabs {
}

.event-grabs__title {
    margin-top: 20px;
    margin-bottom: 4px;
    font-size: 22px;
    color: var(--color-fishbase-whitefish);
    -unity-font-definition: var(--font-bold);
}

.event-grabs__container {
    margin-top: 0px;
}

